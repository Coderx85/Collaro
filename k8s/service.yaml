apiVersion: v1
kind: Service
metadata:
  name: collaro
  namespace: collaro
  labels:
    app: collaro
    environment: production
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: collaro
